<fieldset
  <%= html_attributes(class: "field_with_errors") if @composition.errors.include?(:groups) %>
  id="groups">
  <legend>Groups</legend>

  <% [1,2].each do |group_num| %>
    <%= render partial: "data_explorer/dimensional/group_chooser", locals: {group_num: group_num} %>
  <% end %>
</fieldset>
